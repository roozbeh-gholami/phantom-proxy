name: Build and Release

on:
    workflow_dispatch:
    push:
        tags:
            - "v*.*.*"

permissions:
    contents: write

jobs:
    build-linux:
        runs-on: ubuntu-latest
        container:
            image: golang:1.25
        strategy:
            matrix:
                goarch: [amd64, arm64]

        steps:
            - name: Setup Build Environment
              run: |
                  set -e
                  apt-get update
                  if [ "${{ matrix.goarch }}" = "arm64" ]; then
                    dpkg --add-architecture arm64
                    apt-get update
                    apt-get install -y --no-install-recommends gcc-aarch64-linux-gnu libc6-dev-arm64-cross libpcap-dev:arm64
                  else
                    apt-get install -y --no-install-recommends build-essential libpcap-dev
                  fi

            - name: Checkout Code
              uses: actions/checkout@v5

            - name: Mark repo as safe
              run: git config --global --add safe.directory $GITHUB_WORKSPACE

            - name: Get tag or commit
              id: ref_id
              shell: bash
              run: |
                  if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
                    REF_ID="${GITHUB_REF#refs/tags/}"
                    echo "Triggered by tag: $REF_ID"
                  else
                    REF_ID=$(git rev-parse --short HEAD)
                    echo "No tag detected. Using commit hash: $REF_ID"
                  fi
                  echo "ref=$REF_ID" >> $GITHUB_OUTPUT

            - name: Build
              run: |
                  mkdir -p dist
                  export CGO_ENABLED=1 
                  export GOOS=linux 
                  export GOARCH=${{ matrix.goarch }}
                  export VERSION=${{ steps.ref_id.outputs.ref }}
                  export GIT_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "unknown")
                  export GIT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "unknown")
                  export BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')

                  # Set the C compiler for cross-compilation
                  if [ "$GOARCH" = "arm64" ]; then
                    export CC=aarch64-linux-gnu-gcc
                  fi

                  go build -v -a -trimpath \
                    -gcflags "all=-l=4" \
                    -ldflags "-s -w -buildid= \
                    -X 'phantom-proxy/cmd/version.Version=${VERSION}' \
                    -X 'phantom-proxy/cmd/version.GitCommit=${GIT_COMMIT}' \
                    -X 'phantom-proxy/cmd/version.GitTag=${GIT_TAG}' \
                    -X 'phantom-proxy/cmd/version.BuildTime=${BUILD_TIME}'" \
                    -o phantom-proxy_linux_${{ matrix.goarch }} ./cmd/main.go

                  chmod +x phantom-proxy_linux_${{ matrix.goarch }}
                  tar -czvf dist/phantom-proxy-linux-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.tar.gz \
                    phantom-proxy_linux_${{ matrix.goarch }} \
                    README.md \
                    example/client.yaml.example \
                    example/server.yaml.example

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: phantom-proxy-linux-${{ matrix.goarch }}
                  path: dist/phantom-proxy-linux-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.tar.gz
                  retention-days: 1

    build-windows:
        runs-on: windows-latest
        strategy:
            matrix:
                goarch: [amd64]
        steps:
            - name: Setup Build Environment
              run: |
                  # Install Npcap for libpcap support on Windows
                  choco install npcap -y
                  # Install MinGW for CGO support
                  choco install mingw -y

            - name: Checkout Code
              uses: actions/checkout@v5

            - name: Setup Go
              uses: actions/setup-go@v5
              with:
                  go-version: "1.25"

            - name: Get tag or commit
              id: ref_id
              shell: bash
              run: |
                  if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
                    REF_ID="${GITHUB_REF#refs/tags/}"
                    echo "Triggered by tag: $REF_ID"
                  else
                    REF_ID=$(git rev-parse --short HEAD)
                    echo "No tag detected. Using commit hash: $REF_ID"
                  fi
                  echo "ref=$REF_ID" >> $GITHUB_OUTPUT

            - name: Build
              shell: bash
              run: |
                  mkdir -p dist
                  export CGO_ENABLED=1 
                  export GOOS=windows 
                  export GOARCH=${{ matrix.goarch }}
                  export VERSION=${{ steps.ref_id.outputs.ref }}
                  export GIT_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "unknown")
                  export GIT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "unknown")
                  export BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')

                  go build -v -a -trimpath \
                    -gcflags "all=-l=4" \
                    -ldflags "-s -w -buildid= \
                    -X 'phantom-proxy/cmd/version.Version=${VERSION}' \
                    -X 'phantom-proxy/cmd/version.GitCommit=${GIT_COMMIT}' \
                    -X 'phantom-proxy/cmd/version.GitTag=${GIT_TAG}' \
                    -X 'phantom-proxy/cmd/version.BuildTime=${BUILD_TIME}'" \
                    -o phantom-proxy_windows_${{ matrix.goarch }}.exe ./cmd/main.go

                  7z a -tzip dist/phantom-proxy-windows-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.zip \
                    phantom-proxy_windows_${{ matrix.goarch }}.exe \
                    README.md \
                    example/client.yaml.example \
                    example/server.yaml.example

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: phantom-proxy-windows-${{ matrix.goarch }}
                  path: dist/phantom-proxy-windows-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.zip
                  retention-days: 1

    build-darwin:
        runs-on: macos-latest
        strategy:
            matrix:
                goarch: [amd64, arm64]
        steps:
            - name: Checkout Code
              uses: actions/checkout@v5

            - name: Setup Go
              uses: actions/setup-go@v5
              with:
                  go-version: "1.25"

            - name: Get tag or commit
              id: ref_id
              run: |
                  if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
                    REF_ID="${GITHUB_REF#refs/tags/}"
                    echo "Triggered by tag: $REF_ID"
                  else
                    REF_ID=$(git rev-parse --short HEAD)
                    echo "No tag detected. Using commit hash: $REF_ID"
                  fi
                  echo "ref=$REF_ID" >> $GITHUB_OUTPUT

            - name: Build
              run: |
                  mkdir -p dist
                  export CGO_ENABLED=1 
                  export GOOS=darwin 
                  export GOARCH=${{ matrix.goarch }}
                  export VERSION=${{ steps.ref_id.outputs.ref }}
                  export GIT_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "unknown")
                  export GIT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "unknown")
                  export BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')

                  go build -v -a -trimpath \
                    -gcflags "all=-l=4" \
                    -ldflags "-s -w -buildid= \
                    -X 'phantom-proxy/cmd/version.Version=${VERSION}' \
                    -X 'phantom-proxy/cmd/version.GitCommit=${GIT_COMMIT}' \
                    -X 'phantom-proxy/cmd/version.GitTag=${GIT_TAG}' \
                    -X 'phantom-proxy/cmd/version.BuildTime=${BUILD_TIME}'" \
                    -o phantom-proxy_darwin_${{ matrix.goarch }} ./cmd/main.go

                  chmod +x phantom-proxy_darwin_${{ matrix.goarch }}
                  tar -czvf dist/phantom-proxy-darwin-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.tar.gz \
                    phantom-proxy_darwin_${{ matrix.goarch }} \
                    README.md \
                    example/client.yaml.example \
                    example/server.yaml.example

            - name: Upload Artifact
              uses: actions/upload-artifact@v4
              with:
                  name: phantom-proxy-darwin-${{ matrix.goarch }}
                  path: dist/phantom-proxy-darwin-${{ matrix.goarch }}-${{ steps.ref_id.outputs.ref }}.tar.gz
                  retention-days: 1

    release:
        runs-on: ubuntu-latest
        needs: [build-linux, build-darwin, build-windows]
        if: startsWith(github.ref, 'refs/tags/')
        steps:
            - name: Download all build artifacts
              uses: actions/download-artifact@v4
              with:
                  path: ./dist
                  pattern: phantom-proxy-*
                  merge-multiple: true

            - name: Create Release
              uses: softprops/action-gh-release@v2
              with:
                  files: dist/*
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
